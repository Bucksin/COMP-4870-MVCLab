@{
    string[] files = ViewBag.Files as string[] ?? new string[0];
}

<div class="text-center">
    <h1 class="display-4">Files</h1>
        @foreach (var file in files)
        {
            <p><a asp-controller="File" asp-action="Content" asp-route-id=@file>@file</a></p>
        }

</div>